# Copy to .env.test.local and replace only with disposable test credentials.
# The database name must contain "test"; production mode and production hosts are rejected.
TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5432/student_registration_test
TEST_DATABASE_ALLOWED_HOSTS=localhost,127.0.0.1,::1,postgres
